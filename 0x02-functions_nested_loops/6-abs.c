#include "main.h"
/**
 * _abs - checks for absolut value of int
 *
 * @int: integer output
 * @i: interger
 * Return: absolute value
 */
int _abs(int i)
{

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

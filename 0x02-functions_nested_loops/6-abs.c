#include "main.h"
/**
 * _abs - checks for absolut value of int
 *
 * @i: integer output
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
